CREATE TABLE bookEntity (
    id TEXT NOT NULL PRIMARY KEY,
    title TEXT NOT NULL,
    cover TEXT NOT NULL
);

getBookById:
SELECT *
FROM bookEntity
WHERE id = :id;


insertBook:
INSERT OR REPLACE
INTO bookEntity
VALUES(?,?,?);